/**
 * Module-level constants threaded through every audit-log write the
 * production-remote tools emit. Mirrors `demo-ediscovery-review`'s
 * matter-context shim — see that file for the reasoning.
 */
export const MATTER_ID = 'M-2026-0042';
export const ACTOR = 'production-specialist@firm.example';
