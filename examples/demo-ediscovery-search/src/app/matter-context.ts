/**
 * Matter context shim for the search remote. Mirrors the same pattern
 * used by review and production remotes — module-level constants
 * threaded through audit-log writes.
 */
export const MATTER_ID = 'M-2026-0042';
export const ACTOR = 'search-specialist@firm.example';
