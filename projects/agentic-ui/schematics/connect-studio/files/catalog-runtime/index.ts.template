/**
 * Catalog runtime bridge — connects this app to an Experience Studio catalog
 * backend. Generated by `ng generate @infra-tools/agentic-ui:connect-studio`.
 *
 * Public surface: `provideCatalogRuntime(...)` for `app.config.ts`, the config
 * tokens for OIDC wiring, and the source services (inject their `count` /
 * `error` signals to build a connection-status indicator).
 */
export { provideCatalogRuntime } from './provide-catalog-runtime';
export {
  CATALOG_CONFIG, CATALOG_AUTH,
  type CatalogRuntimeConfig, type ResolvedCatalogConfig, type CatalogAuth,
} from './catalog.config';
export { CatalogClient } from './catalog-client';
export {
  CatalogExperienceSource, CatalogFormSource, CatalogWorkflowSource,
  CatalogDataSource, CatalogToolSource,
} from './sources';
