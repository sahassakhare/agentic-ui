{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "MaverickAgenticUiChatShell",
  "title": "Generate a chat shell route + component",
  "type": "object",
  "properties": {
    "name": { "type": "string", "default": "Chat", "$default": { "$source": "argv", "index": 0 } },
    "project": { "type": "string", "$default": { "$source": "projectName" } },
    "path": { "type": "string", "default": "app/chat" },
    "prefix": { "type": "string", "default": "app" }
  },
  "required": []
}
