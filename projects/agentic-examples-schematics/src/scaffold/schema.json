{
  "$schema": "http://json-schema.org/schema",
  "$id": "AgenticExamplesScaffold",
  "title": "Scaffold the Agentic Experience Platform example apps",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "Target directory the examples are scaffolded into.",
      "default": "."
    },
    "overwrite": {
      "type": "boolean",
      "description": "Overwrite files that already exist in the target.",
      "default": false
    }
  },
  "required": []
}
