{
  "$schema": "http://json-schema.org/schema",
  "$id": "AgenticPlatformScaffold",
  "title": "Scaffold the Agentic Experience Platform monorepo",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "Target directory the workspace is scaffolded into.",
      "default": "."
    },
    "includeExamples": {
      "type": "boolean",
      "description": "Include the examples/ apps and MFE remotes.",
      "default": true
    },
    "overwrite": {
      "type": "boolean",
      "description": "Overwrite files that already exist in the target.",
      "default": false
    }
  },
  "required": []
}
