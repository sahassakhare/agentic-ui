{
  "name": "@infra-tools/component-ingest-service",
  "version": "0.1.0",
  "private": true,
  "description": "Ingest an external Angular component library (npm/zip/tarball), build it into a Native Federation remote of agenticWidgets, and register the components in the catalog — no host redeploy.",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0",
    "typescript": "~5.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "vitest": "^4.1.0"
  }
}
